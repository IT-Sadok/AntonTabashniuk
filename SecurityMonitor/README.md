# Architecture

This repository is organized as a monorepo.

Services:

- SecurityMonitor.Identity
- SecurityMonitor

Each service has:
- its own Api
- its own Application
- its own Domain
- its own Infrastructure
- its own PostgreSQL database